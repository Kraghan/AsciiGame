#pragma once
#include "Window.h"
#include "Vector2.h"

class AlphabetDrawer
{
public : 
	static void drawA(Window* window, Vector2 pos);
	static void drawB(Window* window, Vector2 pos);
	static void drawC(Window* window, Vector2 pos);
	static void drawD(Window* window, Vector2 pos);
	static void drawE(Window* window, Vector2 pos);
	static void drawF(Window* window, Vector2 pos);
	static void drawG(Window* window, Vector2 pos);
	static void drawH(Window* window, Vector2 pos);
	static void drawI(Window* window, Vector2 pos);
	static void drawJ(Window* window, Vector2 pos);
	static void drawK(Window* window, Vector2 pos);
	static void drawL(Window* window, Vector2 pos);
	static void drawM(Window* window, Vector2 pos);
	static void drawN(Window* window, Vector2 pos);
	static void drawO(Window* window, Vector2 pos);
	static void drawP(Window* window, Vector2 pos);
	static void drawQ(Window* window, Vector2 pos);
	static void drawR(Window* window, Vector2 pos);
	static void drawS(Window* window, Vector2 pos);
	static void drawT(Window* window, Vector2 pos);
	static void drawU(Window* window, Vector2 pos);
	static void drawV(Window* window, Vector2 pos);
	static void drawW(Window* window, Vector2 pos);
	static void drawX(Window* window, Vector2 pos);
	static void drawY(Window* window, Vector2 pos);
	static void drawZ(Window* window, Vector2 pos);
};